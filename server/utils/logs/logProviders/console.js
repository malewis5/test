const initialize = () => {}

const hook = () => {
  //do nothing
}

const logMessage = (message) => {
  console.log(message)
}

const logError = (error) => {
  console.error(error)
}

module.exports = { initialize, logMessage, logError, hook }
